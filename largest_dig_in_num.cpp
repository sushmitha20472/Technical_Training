#include<stdio.h>
int main()
{
	int n,m,sum,l;
	scanf("%d",&n);
	while(n>0)
	{
		m=n%10;
		sum = sum+m;
		n=n/10;
	if(s>0)
	{
		printf("\nIlargest digit is\n",n);
	}
	else
	{
		printf("%d is not largest\n",n);
	}
}
	return 0;
}
